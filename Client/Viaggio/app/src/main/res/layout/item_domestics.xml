<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools" xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="android.view.View"/>
        <import type="com.kotlin.viaggio.data.obj.Area"/>

        <variable name="data" type="Area"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">
        <TextView
                android:id="@id/domesticsName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="와이너리"
                android:layout_margin="5dp"
                android:textColor="@{data.selected ? @color/colorPrimary:@color/almost_black}"
                android:text="@{data.city}"
                android:textSize="14dp"
                android:gravity="center"
                android:paddingStart="@dimen/common_top"
                android:paddingEnd="@dimen/common_top"
                android:paddingTop="6.8dp"
                android:paddingBottom="6.8dp"
                android:background="@{data.selected ? @drawable/selected_theme_btn_bg:@drawable/theme_btn_bg}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                tools:ignore="SpUsage"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>