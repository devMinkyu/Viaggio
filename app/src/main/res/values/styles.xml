<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Base.MinV.ViaggioAppTheme.Dialog" parent="Theme.AppCompat.Dialog">
        <!-- hides status bar on top with clock and wifi status -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="Base.ViaggioAppTheme.Dialog" parent="Base.MinV.ViaggioAppTheme.Dialog"/>

    <style name="ViaggioAppTheme.Dialog" parent="Base.ViaggioAppTheme.Dialog"/>

    <style name="ViaggioAppTheme.Dialog.Opaque">
        <!--뒷 배경 어둡게 만드는것-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
